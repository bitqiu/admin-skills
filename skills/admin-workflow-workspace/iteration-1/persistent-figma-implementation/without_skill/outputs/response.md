路由决定：选择“持久化 Figma 实现”路径。先以可访问的 Figma 用户管理 frame、现有 `ADMIN_SPEC.md` 和 React 项目约束为输入，提炼后台通用的设计令牌、组件模式与页面规则并生成 `DESIGN.md`；再以 Figma 为视觉真源、同时遵循 `ADMIN_SPEC.md` 与新建的 `DESIGN.md`，路由到用户管理页实现与视觉验证。

理由：`DESIGN.md` 缺失且后续整个后台都要复用这套设计，因此不应走一次性页面实现路径；先持久化共享设计规范，才能让当前页面和后续页面保持一致。本次只作路由判断，不执行规范生成、代码修改或验证。
